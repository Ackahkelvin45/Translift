// Direct UI copy + precision traps (className noise, an attribute sink).
export const Banner = () => (
  <div className="banner wrapper-large">
    <h1>Welcome to your dashboard</h1>
    <p>You have new messages waiting.</p>
    <button aria-label="Close notification">Dismiss</button>
  </div>
);
